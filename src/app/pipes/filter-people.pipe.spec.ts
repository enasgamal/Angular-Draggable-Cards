import { FilterPeoplePipe } from './filter-people.pipe';

describe('FilterPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
